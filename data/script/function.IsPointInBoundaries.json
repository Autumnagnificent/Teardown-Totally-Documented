{
  "name": "IsPointInBoundaries",
  "arguments": [
    {
      "name": "point",
      "desc": "Point",
      "optional": false,
      "type": "TVec"
    }
  ],
  "returns": [
    {
      "name": "value",
      "desc": "True if point is inside scene boundaries",
      "optional": false,
      "type": "boolean"
    }
  ],
  "examples": [
    [
      "function tick()",
      "\tlocal p = Vec(1.5, 3, 2.5)",
      "\tDebugWatch(\"In boundaries\", IsPointInBoundaries(p))",
      "end"
    ]
  ],
  "description": [
    "Checks whether the point is within the scene boundaries.",
    "If there are no boundaries on the scene, the function returns True."
  ]
}